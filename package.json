{
  "name": "datarust-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Static documentation and blog site for datarust",
  "scripts": {
    "build": "node site/build.mjs",
    "check": "node site/check.mjs",
    "preview": "node site/serve.mjs",
    "deploy": "npm run build && npx wrangler pages deploy dist"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "marked": "18.0.7"
  }
}
